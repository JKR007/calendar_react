%h1 React Calendar

%h2 Appointments

%h3 Make a new appointment
= form_for @appointment, remote: true do |f|
  = f.label 'Title'
  = f.text_field :title

  = f.label 'Date\Time'
  = f.text_field :appointment_time
  = f.submit 'Make an Appointment'
#appointments
  = render 'appointments'
